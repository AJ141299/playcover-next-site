const KeysVersion = ({ version, text }) => {
  return (<p className="pl-3 mb-10 border-l-4 rounded-sm border-yellow-500 dark:border-yellow-600">{ text } { version }</p>);
};

export default KeysVersion;